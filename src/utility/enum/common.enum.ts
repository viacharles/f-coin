export enum ESize {
  XXS = 'xxs',
  XS = 'xs',
  Small = 'sm',
  Middle = 'md',
  Large = 'lg',
  XL = 'xl',
  XXL = 'xxl',
  FitContent = 'fit-content'
}

export enum EDevice {
  Desktop = 'desktop',
  Tablet = 'tablet',
  Mobile = 'mobile'
}

export enum EModalType {
  Alert = 'alert',
  Action = 'normal',
  Notification = 'info'
}

export enum EAction {
  Initial = 1,
  Add,
  Modify,
  Delete,
  Cancel,
  Save,
  Clear,
  Apply,
  Preview,
  Submit
}

